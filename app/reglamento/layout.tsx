import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reglamento | Safari Tras las Sierras 2026',
  description: 'Reglamento particular de la prueba - Safari Tras las Sierras 2026. Valle Fértil, San Juan.',
};

export default function ReglamentoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
